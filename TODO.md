# TODO - Home Page UI/UX improvements

- [ ] Inspect current `frontend/src/pages/Public/Home.jsx` and `frontend/src/pages/Public/Home.css` (done).
- [x] Update `Home.css` to better align with `frontend/src/index.css` design tokens and improve typography/spacing.
- [x] Add subtle, production-ready animations with `prefers-reduced-motion` fallback.

- [ ] Improve accessibility: focus-visible styles for interactive elements, semantic fixes where possible (without changing shared Header/Footer).
- [ ] Refine hero visual hierarchy and responsive behavior (mobile-first polish).
- [ ] Improve section spacing, hover/focus consistency for feature cards, statistics, testimonials, and footer links.
- [ ] Run `frontend` lint + build to ensure everything compiles.

